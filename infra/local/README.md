# Local Configuration

This folder contains configuration files for running AgenticLab entirely locally.

## Files

| File | Purpose |
|------|---------|
| `appsettings.local.json` | Local runtime configuration |

## Usage

Copy the appropriate configuration file to the demo project or set the environment variable:

```bash
AGENTICLAB_CONFIG=infra/local/appsettings.local.json
```
